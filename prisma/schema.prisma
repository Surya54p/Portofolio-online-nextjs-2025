datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}
model Like {
  id        Int      @id @default(autoincrement())
  nama      String
  createdAt DateTime @default(now())
}
